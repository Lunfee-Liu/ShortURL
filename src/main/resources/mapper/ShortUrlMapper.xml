<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shorturl.mapper.ShortUrlMapper">

    <resultMap id="BaseResultMap" type="com.example.shorturl.entity.ShortUrlDO">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="short_code" property="shortCode" jdbcType="VARCHAR"/>
        <result column="original_url" property="originalUrl" jdbcType="VARCHAR"/>
        <result column="shard_key" property="shardKey" jdbcType="VARCHAR"/>
        <result column="is_deleted" property="isDeleted" jdbcType="TINYINT"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, short_code, original_url, shard_key, is_deleted, created_at, updated_at
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id"
            parameterType="com.example.shorturl.entity.ShortUrlDO">
        INSERT INTO short_urls (short_code, original_url, shard_key, is_deleted, created_at, updated_at)
        VALUES (#{shortCode,jdbcType=VARCHAR},
                #{originalUrl,jdbcType=VARCHAR},
                #{shardKey,jdbcType=VARCHAR},
                #{isDeleted,jdbcType=TINYINT},
                #{createdAt,jdbcType=TIMESTAMP},
                #{updatedAt,jdbcType=TIMESTAMP})
    </insert>

    <select id="selectByPrimaryKey" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM short_urls
        WHERE id = #{id,jdbcType=BIGINT}
          AND is_deleted = 0
    </select>

    <select id="selectByShortCode" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM short_urls
        WHERE short_code = #{shortCode,jdbcType=VARCHAR}
          AND is_deleted = 0
    </select>

    <update id="updateByPrimaryKey"
            parameterType="com.example.shorturl.entity.ShortUrlDO">
        UPDATE short_urls
        SET short_code   = #{shortCode,jdbcType=VARCHAR},
            original_url = #{originalUrl,jdbcType=VARCHAR},
            shard_key    = #{shardKey,jdbcType=VARCHAR},
            is_deleted   = #{isDeleted,jdbcType=TINYINT},
            updated_at   = #{updatedAt,jdbcType=TIMESTAMP}
        WHERE id = #{id,jdbcType=BIGINT}
    </update>

    <!-- logical delete: set is_deleted = 1 instead of physical DELETE -->
    <update id="deleteByPrimaryKey">
        UPDATE short_urls
        SET is_deleted = 1
        WHERE id = #{id,jdbcType=BIGINT}
    </update>

    <!-- Admin: paginated list with visit_count aggregated from access_logs -->
    <resultMap id="AdminShortUrlVOMap" type="com.example.shorturl.vo.AdminShortUrlVO">
        <id     column="id"           property="id"/>
        <result column="short_code"   property="shortCode"/>
        <result column="original_url" property="originalUrl"/>
        <result column="visit_count"  property="visitCount"/>
        <result column="created_at"   property="createdAt"/>
        <result column="updated_at"   property="updatedAt"/>
    </resultMap>

    <sql id="Admin_Where">
        WHERE s.is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (s.short_code LIKE CONCAT('%', #{keyword}, '%')
              OR s.original_url LIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </sql>

    <select id="selectAdminPage" resultMap="AdminShortUrlVOMap">
        SELECT s.id,
               s.short_code,
               s.original_url,
               s.created_at,
               s.updated_at,
               COALESCE(a.visit_count, 0) AS visit_count
        FROM short_urls s
        LEFT JOIN (
            SELECT short_code, COUNT(*) AS visit_count
            FROM access_logs
            WHERE is_deleted = 0
            GROUP BY short_code
        ) a ON s.short_code = a.short_code
        <include refid="Admin_Where"/>
        ORDER BY s.created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countAdmin" resultType="long">
        SELECT COUNT(*)
        FROM short_urls s
        <include refid="Admin_Where"/>
    </select>

</mapper>
