spring:
  datasource:
    url: "${DATASOURCE_URL}"
    username: "${DATASOURCE_USERNAME}"
    password: "${DATASOURCE_PASSWORD}"

  data:
    redis:
      host: "${REDIS_HOST}"
      port: "${REDIS_PORT}"

logging:
  level:
    com.example.shorturl: INFO
